meta {
  name: GET point de collecte - 404 inexistant
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/points/9999
  body: none
  auth: none
}

tests {
  test("status 404", function() {
    expect(res.getStatus()).to.equal(404);
  });
}
