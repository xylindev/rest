meta {
  name: GET points surchargés (> 80%)
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/points/overloaded
  body: none
  auth: none
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("réponse est un tableau", function() {
    expect(res.getBody()).to.be.an("array");
  });
}
