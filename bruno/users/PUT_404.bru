meta {
  name: PUT utilisateur - 404 inexistant
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/users/9999
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "login": "fantome",
    "password": "secret",
    "role": "USER"
  }
}

tests {
  test("status 404", function() {
    expect(res.getStatus()).to.equal(404);
  });
}
