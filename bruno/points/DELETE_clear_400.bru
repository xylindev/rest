meta {
  name: DELETE vider un point - 400 mauvaise URL
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/points/1
  body: none
  auth: none
}

tests {
  test("status 400 sans /clear", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
