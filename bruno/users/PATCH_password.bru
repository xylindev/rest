meta {
  name: PATCH modifier partiellement un utilisateur
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/users/2
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "password": "nouveauSecret"
  }
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
