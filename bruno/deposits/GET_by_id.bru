meta {
  name: GET dépôt par ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/deposits/1
  body: none
  auth: none
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("id correct", function() {
    expect(res.getBody().id).to.equal(1);
  });
}
