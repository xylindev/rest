meta {
  name: DELETE type de déchet - 409 utilisé dans dépôts
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/waste-types/1
  body: none
  auth: none
}

tests {
  test("status 409 conflit", function() {
    expect(res.getStatus()).to.equal(409);
  });
}
