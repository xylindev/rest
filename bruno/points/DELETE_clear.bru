meta {
  name: DELETE vider les dépôts d'un point
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/points/3/clear
  body: none
  auth: none
}

tests {
  test("status 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
